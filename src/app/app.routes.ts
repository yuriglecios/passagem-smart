import { Routes } from '@angular/router';
import { FormPassagemComponent } from './componentes/form-passagem/form-passagem.component';

export const routes: Routes = [
    { path: '', component: FormPassagemComponent }
];
